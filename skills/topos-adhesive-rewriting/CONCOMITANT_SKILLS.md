<!-- BEGIN GENERATED related (scripts/relate_skills.py) -->

## Related

Skills related to this one, with the nature of each relation (direction from prose citations; cluster domains characterized from exa and our own use). Symmetric — each related skill lists this one too. Do not edit inside the markers; regenerate with `python3 scripts/relate_skills.py`.

- `acsets-relational-thinking` — builds on — ACSets (Attributed C Sets) for categorical database design and DPO rewriting
- `algebraic-rewriting` — sibling in the categorical cybernetics — parametrised optics & bidirectional learners cluster — Category theoretic graph rewriting with DPO, SPO, and SqPO pushouts for C Sets.
- `bkp-interleaving` — sibling in the categorical cybernetics — parametrised optics & bidirectional learners cluster — This skill is the **deep interleaving** of the five BKP 2 monad theory skills:
- `catlab-asi-interleave` — invoked by — Bridge layer connecting AlgebraicJulia Catlab.jl to plurigrid asi.
- `codescent` — invoked by — A **codescent object** is the 2 categorical analogue of a coequalizer.
- `coequalizers` — sibling in the categorical cybernetics — parametrised optics & bidirectional learners cluster — Quotient redundant skill paths via coequalizers, preserving GF(3) conservation
- `duckdb-temporal-versioning` — builds on — Temporal versioning and interaction history with time travel queries,
- `flexible-algebra` — sibling in the categorical cybernetics — parametrised optics & bidirectional learners cluster — A **flexible T algebra** is a strict T algebra that admits enough "room to move" — it is …
- `gh-interactome` — builds on — GitHub author interaction network discovery.
- `glass-bead-game` — sibling in the topos theory, sheaves & categorical logic cluster — Hesse inspired interdisciplinary synthesis game with Badiou triangle
- `julia-gpu-kernels` — builds on — KernelAbstractions.jl: Backend agnostic GPU kernel programming for Julia.
- `string-diagram-rewriting-protocol` — sibling in the categorical cybernetics — parametrised optics & bidirectional learners cluster — Kernel protocol for compositional string diagram rewriting across all skills
- `three-match` — sibling in the topos theory, sheaves & categorical logic cluster — 3 MATCH colored subgraph isomorphism gadget for 3 SAT reduction
- `unworld` — sibling in the topos theory, sheaves & categorical logic cluster — Layer 4: Derivational Pattern Generation via Seed Chaining

<!-- END GENERATED related -->
