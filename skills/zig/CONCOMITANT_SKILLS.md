<!-- BEGIN GENERATED related (scripts/relate_skills.py) -->

## Related

Skills related to this one, with the nature of each relation (direction from prose citations; cluster domains characterized from exa and our own use). Symmetric — each related skill lists this one too. Do not edit inside the markers; regenerate with `python3 scripts/relate_skills.py`.

- `abductive-oracle` — invoked by — Formal oracle for abductive inference that selects the minimal hypothesis best explaining…
- `acp-commons` — invoked by — Atlas of Agent Client Protocol (ACP) clients, agents, and OCapN CapTP grounded actors.
- `acsets` — builds on — ACSets (Attributed C Sets): Algebraic databases with Specter style bidirectional
- `asi-transient-agenda` — invoked by — Org agenda like transient views for ASI skill orchestration via nbb squint + Emacs hydra
- `babashka` — builds on — Clojure scripting without JVM startup.
- `bandwidth-benchmark` — invoked by — Measure encoding decoding throughput across Syrup implementations.
- `belief-revision-log` — invoked by — > Persistent belief revision log wiring in memory AGM BeliefSet to DuckDB time travel sto…
- `bisimulation-oracle` — invoked by — > Formal oracle for behavioral equivalence via bisimulation games.
- `bob-emacs-mods` — invoked by — Complement to alice emacs mods.
- `botnet-disruption` — invoked by — Coordinated botnet takedown methodologies — sinkholing, infrastructure mapping, legal fra…
- `botnet-studies` — invoked by — Botnet architecture taxonomy, detection techniques, and defensive analysis.
- `cat` — builds on — cat Skill: Derivational Pipe Chaining
- `cross-runtime-exchange` — invoked by — This skill enables content addressed interoperability between three Syrup implementations:
- `dafny-zig` — sibling in the Zig systems programming cluster — Dafny to Zig compiler backend development skill with verified runtime safety ("Zig syrup")
- `deep-inference-interleave` — invoked by — Compositional passive inference vs emergent active inference (Hedges Feb 2024) — chain ru…
- `duckdb-ies` — builds on — Layer 4: IES Interactome Analytics with GF(3) Momentum Tracking
- `flow` — builds on — One parameter group of diffeomorphisms generated by vector field
- `gay-mcp` — builds on — Deterministic color generation with SplitMix64, GF(3) trits, and MCP
- `gf3-trit-oracle` — invoked by — > The fundamental GF(3) trit oracle.
- `glamorous-moldable-multiplatform` — invoked by — - **v1.1.0 (2026 02 09)**: Live GraphQL update of all 10 repos with commit level detail.
- `indefinite-causal-order` — invoked by — Indefinite causal order (ICO) is the framework where the causal relationship
- `init` — builds on — Initialize a new repository with AGENTS.md
- `jo-clojure` — invoked by — Clojure skill orchestration and polyglot bridge.
- `libghostty-aci` — invoked by — Integrates libghostty terminal emulation with Agent Computer Interface (ACI) patterns:
- `libghostty-embed` — invoked by — libghostty terminal embedding for per vat REPLs with Goblins integration
- `libghostty-recording` — invoked by — Record, stream, and replay libghostty vt terminal sessions for documentation, debugging, …
- `libghostty-streaming` — invoked by — The **white hole** dual to libghostty recording.
- `libghostty-vt` — invoked by — libghostty vt
- `nashator` — invoked by — Open games DSL Nash equilibrium solver via PyTorch autodiff.
- `neurofeedback-oracle` — invoked by — > Formal oracle mapping EEG derived focus scores to trits via fixed thresholds.
- `olmo-earth-skill` — invoked by — Sentinel 2 satellite imagery to OlmoEarth embeddings on geodesic tiles, serialized via zi…
- `open-location-code-zig` — sibling in the Zig systems programming cluster — First ever Zig implementation of Google's Open Location Code (Plus Codes) with GF(3) spat…
- `plurigrid-polyglot` — invoked by — Polyglot skill orchestration system integrating Go, Clojure, Hy, and Zig.
- `polyglot-spi` — invoked by — Cross Language Strong Parallelism Invariance Verification
- `propagators` — builds on — Sussman Radul propagator networks for constraint propagation and bidirectional dataflow.
- `sdf` — mutually referenced — Software Design for Flexibility: Sussman & Hanson's additive programming, combinators, pr…
- `semilattice-discovery` — invoked by — Compositional skill discovery via join semilattice fiber (color × trit × hash)
- `servo-ghostty` — invoked by — Servo browser engine integration with ghostty web for full color terminal tiles
- `string-diagram-rewriting-protocol` — invoked by — Kernel protocol for compositional string diagram rewriting across all skills
- `structured-decomp` — builds on — StructuredDecompositions.jl sheaves on tree decompositions for FPT algorithms with bidire…
- `terminal` — builds on — Terminal tools = tmux + zsh + fzf + ripgrep.
- `transitive-weep` — invoked by — Each world is an **open game** with:
- `zig-programming` — sibling in the Zig systems programming cluster — >
- `zig-syrup-bci` — sibling in the Zig systems programming cluster — Multimodal BCI pipeline in Zig: DSI 24 EEG, fNIRS mBLL, eye tracking IVT, LSL sync, EDF r…
- `zig-syrup-propagator-interleave` — sibling in the Zig systems programming cluster — > Bridge connecting zig syrup advanced computational modules (propagator networks, homoto…
- `zig-systems` — sibling in the Zig systems programming cluster — Systems programming and performance optimization using Zig.
- `zls-integration` — mutually referenced — zls integration skill Zig Language Server (ZLS) integration for IDE features - autocomple…

<!-- END GENERATED related -->
