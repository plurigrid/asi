<!-- BEGIN GENERATED related (scripts/relate_skills.py) -->

## Related

Skills related to this one, with the nature of each relation (direction from prose citations; cluster domains characterized from exa and our own use). Symmetric — each related skill lists this one too. Do not edit inside the markers; regenerate with `python3 scripts/relate_skills.py`.

- `bisimulation-game` — invoked by — Bisimulation game for resilient skill dispersal across AI agents with
- `bmorphism-ies-env` — invoked by — Documentation and orchestration for the bmorphism ies flox environment toolkit
- `captp` — sibling in the Spritely distributed object capability programming (CapTP OCapN) cluster — CapTP: Capability Transfer Protocol
- `cheerpx-self-hosting` — invoked by — Run x86 Linux binaries in the browser via WebAssembly.
- `collaborative-emacs` — invoked by — Claude Code runs an Emacs daemon, starts a CRDT server, shares buffers.
- `flox` — invoked by — Reproducible development environments powered by Nix.
- `gotosocial-apple-container` — invoked by — Run a local ActivityPub server on macOS using Apple container CLI (not Docker Colima).
- `ies` — invoked by — ies
- `ies-flox` — invoked by — FloxHub publication bmorphism ies - a focused development environment
- `livestream` — sibling in the Spritely distributed object capability programming (CapTP OCapN) cluster — Warehouse audio pipeline for live capture, transcription, and narration from meeting room…
- `localsend-mcp` — sibling in the Spritely distributed object capability programming (CapTP OCapN) cluster — LocalSend based P2P transfer with MCP server design for NATS Tailscale
- `mcp-from-prompt` — invoked by — Generate a complete MCP server + skill from an API description, following the ExoPriors S…
- `nerv` — invoked by — NERV - Rapid LocalSend Test with Voice
- `port-resurrection` — invoked by — Derive service ports from Gay.jl color sequence.
- `resource-sharing` — invoked by — Resource Sharing Skill
- `scum-score` — invoked by — SCUM Score Skill
- `ssh` — builds on — Establish and manage SSH connections to remote machines, including key generation, config…
- `tailscale-file-transfer` — sibling in the Spritely distributed object capability programming (CapTP OCapN) cluster — Tailscale mesh VPN file transfer with open games semantics (play coplay)
- `tailscale-localsend` — sibling in the Spritely distributed object capability programming (CapTP OCapN) cluster — Tailscale + LocalSend Peer Discovery
- `tailscale-mesh` — sibling in the Spritely distributed object capability programming (CapTP OCapN) cluster — Tailscale mesh VPN for secure peer to peer networking.
- `trifurcated-transfer` — invoked by — Trifurcated Transfer Skill
- `warehouse-network` — sibling in the Spritely distributed object capability programming (CapTP OCapN) cluster — Connect to warehouse machines (DGX Spark, etc.) via local network or Tailscale.

<!-- END GENERATED related -->
