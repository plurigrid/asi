<<<<<<< HEAD
{:metadata
 {:total-skills 485
  :coalgebra-percentage 99
  :analysis-date "2026-01-12"
  :method "Random walk verification via yb-translator"
  :top-coalgebras 34}

 :top-coalgebras
 [{:skill "yb-translator"
   :score 100
   :ct-structure "Full coalgebra with Δ, ε, bicomodule, Kan extensions"
   :comultiplication "Concept → Multiple biological examples"
   :counit "Extract category theory term name/formula"
   :bio-analog "Ontology term → .children.all() decomposition"
   :interpretation "Translates abstract math to concrete biology"}

  {:skill "acsets"
   :score 80
   :ct-structure "Coalgebra + Adjunction (Specter bidirectional)"
   :comultiplication "ACSet schema → parts (V, E, morphisms)"
   :counit "Extract part identifier/name"
   :bio-analog "Cell Ontology term → cell subtypes"
   :interpretation "C-set functor: C → Set (generalizes graphs + dataframes)"
   :left-coaction "Navigate up hierarchy (parents)"
   :right-coaction "Navigate down hierarchy (children)"}

  {:skill "hvm-runtime"
   :score 80
   :ct-structure "Coalgebra with parallel decomposition"
   :comultiplication "Computation → parallel reduction branches"
   :counit "Extract normal form result"
   :bio-analog "Metabolic pathway → parallel reaction branches"
   :interpretation "Optimal β-reduction as coalgebra dynamics"}

  {:skill "_integrated"
   :score 70
   :ct-structure "Left Kan extension (aggregation of subsystems)"
   :comultiplication "Unified system → component skills"
   :counit "Extract system identity"
   :bio-analog "Organism → organ systems → tissues → cells"
   :interpretation "Lan_F: Aggregate child skills into unified parent"
   :subsystems ["ACSets" "Gay-MCP" "Bisimulation" "World-hopping" "Glass-bead"]}

  {:skill "aqua-voice-malleability"
   :score 70
   :ct-structure "Bicomodule + Adjunction"
   :comultiplication "Electron app → IPC channels + WebSocket streams"
   :counit "Extract process/channel ID"
   :bio-analog "Neuron: dendritic input (left) + axonal output (right)"
   :left-coaction "Input audio streams (microphone)"
   :right-coaction "Output audio routing (speakers/loopback)"
   :interpretation "Audio flow as bicomodule between input and output contexts"}

  {:skill "discopy"
   :score 70
   :ct-structure "Monoidal category + Adjunction"
   :comultiplication "Diagram → tensor components"
   :counit "Extract diagram identity/name"
   :bio-analog "Protein complex → subunit proteins"
   :interpretation "String diagrams for composable processes"
   :monoidal-product "⊗ for parallel composition"}

  {:skill "discopy-operads"
   :score 70
   :ct-structure "Operad (colored) + Comultiplication"
   :comultiplication "Operation → input/output wires"
   :counit "Extract operation signature"
   :bio-analog "Enzyme → substrate binding sites + product sites"
   :interpretation "Operadic composition: plug outputs into inputs"}

  {:skill "planar-isotopy-screen"
   :score 70
   :ct-structure "Bicomodule (screen space + mathematical space)"
   :comultiplication "Isotopy → deformation steps"
   :counit "Extract topological invariant"
   :bio-analog "Cell morphology deformation (maintaining identity)"
   :left-coaction "Navigate in screen coordinates"
   :right-coaction "Navigate in mathematical/topological space"
   :interpretation "Planar diagram manipulation preserving isotopy class"}

  {:skill "asi-polynomial-operads"
   :score 65
   :ct-structure "Polynomial functor + Operad"
   :comultiplication "Polynomial → monomials (constituent parts)"
   :counit "Extract leading coefficient/degree"
   :bio-analog "Gene regulatory network → individual regulation edges"
   :interpretation "Polynomial functors model systems with positions/directions"
   :lens-structure "Bidirectional get/put operations"}

  {:skill "catsharp"
   :score 65
   :ct-structure "Musical scale as coalgebra"
   :comultiplication "Scale → pitch classes (notes)"
   :counit "Extract root note/tonic"
   :bio-analog "Species → subspecies (taxonomic hierarchy)"
   :interpretation "Mazzola's categorical music theory: scales as functors"
   :galois-connection "Pitch ⊣ Interval adjunction"}]

 :coalgebra-patterns
 {:comultiplication-examples
  ["Skill → subskills"
   "Schema → parts"
   "Diagram → components"
   "Process → steps"
   "Tree → children nodes"
   "Pathway → reaction steps"
   "System → subsystems"]

  :counit-examples
  ["Extract name/identifier"
   "Get root element"
   "Retrieve signature"
   "Access primary key"
   "Obtain label"]

  :bicomodule-examples
  ["Input/Output contexts"
   "Screen space / Math space"
   "Syntax / Semantics"
   "Type / Value"
   "Genotype / Phenotype"]

  :kan-extension-examples
  {:left-kan ["Aggregate children → parent"
              "Summarize components"
              "Collect properties"]
   :right-kan ["Distribute parent → children"
               "Specialize to instances"
               "Adapt to contexts"]}}

 :biological-ontology-correspondences
 {:cell-ontology
  {:examples ["acsets → Cell types with lineage"
              "_integrated → Multicellular organism"
              "aqua-voice → Neuron with synaptic connections"]
   :structure "Hierarchy: stem cell → differentiated cells"}

  :gene-ontology
  {:examples ["discopy → Biological processes"
              "hvm-runtime → Metabolic pathways"
              "asi-polynomial-operads → Gene regulatory networks"]
   :structure "Process decomposition: biological_process → metabolic → specific pathways"}

  :disease-ontology
  {:examples ["yb-translator → Disease classification"
              "_integrated → Syndrome (multiple subsystems)"]
   :structure "Parent disease → disease subtypes"}

  :tissue-ontology
  {:examples ["planar-isotopy → Anatomical structure deformation"
              "_integrated → Organ systems"]
   :structure "Whole → parts decomposition: lung → lobes"}

  :organism-taxonomy
  {:examples ["catsharp → Phylogenetic tree (species hierarchy)"]
   :structure "Kingdom → Phylum → Class → Order → Family → Genus → Species"}}

 :non-coalgebras
 [{:skill "mitm"
   :score 0
   :reason "Pure data collection without decomposition structure"
   :ct-type "Set (discrete category, no morphisms)"
   :bio-analog "Individual measurement (no substructure)"}]}
=======
;; YB-Translator Skill Taxonomy
;; Generated by analyze_all_skills.clj with structural criteria
;; NOT regex keyword matching. See "What was removed" in SKILL.md.
;;
;; A skill has coalgebra structure iff it defines an explicit typed hierarchy ≥2 levels.
;; A skill has bicomodule structure iff it names two distinct ontological contexts
;; with independent coactions and a compatibility condition.
;; Everything else gets :none. If this file says >20% structured, the criteria are too loose.

{:metadata
 {:method "Structural analysis: explicit hierarchy OR dual ontological context"
  :previous-method "Regex keyword matching (99% positive rate — removed)"
  :note "Most skills have no biological ontology structure. That's correct."}

 ;; Only yb-translator itself has verified coalgebra structure with real ontology IDs.
 ;; Other skills may have coalgebra-LIKE structure but without ontology grounding.
 :verified-translations
 [{:concept "Class inheritance"
   :biology "Cell differentiation hierarchy"
   :id "CL:0000084"
   :parallel :direct
   :mechanism "Both are typed hierarchies: parent type → specialized subtypes"}

  {:concept "Automatic memory management"
   :biology "Autophagy"
   :id "GO:0006914"
   :parallel :direct
   :mechanism "Both reclaim resources by degradation of unused components"}

  {:concept "Immutable data structures"
   :biology "DNA template strand"
   :id "GO:0006260"
   :parallel :direct
   :mechanism "Original preserved during copy; new strand synthesized"}

  {:concept "Compile-time type verification"
   :biology "Receptor-ligand specificity"
   :id "GO:0005102"
   :parallel :direct
   :mechanism "Shape verified before signal transduction / execution"}

  {:concept "Self-referential function"
   :biology "Fractal branching morphogenesis"
   :id "GO:0001763"
   :parallel :direct
   :mechanism "Same rule applied at each level of nesting"}

  {:concept "Code as data"
   :biology "Self-replicating RNA polymerase ribozymes"
   :id "GO:0097747"
   :parallel :direct
   :mechanism "RNA simultaneously catalyst (program) and template (data)"}

  {:concept "Interface contract"
   :biology "Enzyme classification by function"
   :id "GO:0016301"
   :parallel :analogical
   :mechanism "All kinases transfer phosphate (same interface), different substrates"}

  {:concept "Parallel execution"
   :biology "Parallel metabolic pathways"
   :id "GO:0008152"
   :parallel :analogical
   :mechanism "Glycolysis and beta-oxidation run simultaneously but in different compartments"}

  {:concept "Interactive evaluation loop"
   :biology "Adaptive immune response"
   :id "GO:0002250"
   :parallel :analogical
   :mechanism "Encounter → test → remember pattern, but immune memory is not a REPL"}

  {:concept "Function precondition"
   :biology "Enzyme substrate specificity"
   :id "GO:0022838"
   :parallel :analogical
   :mechanism "Wrong substrate = no reaction, but enzymes don't 'check' — they just don't bind"}

  {:concept "Function postcondition"
   :biology "Enzyme product guarantee"
   :id "GO:0003824"
   :parallel :analogical
   :mechanism "Catalysis guarantees products, but no 'verification step' — just thermodynamics"}

  {:concept "Compile-time proof of correctness"
   :biology "Immune checkpoint verification"
   :id "GO:0042110"
   :parallel :analogical
   :mechanism "T cell requires MHC + costimulation, but this is activation gating not proof"}]}
>>>>>>> origin/main
